#include "tool.h"

//put your code here, gets called every frame
int tool::main(int hook_mode)
{	
	/*
	if (hook_mode == 1) {
		MessageBox(0, L"MODE_TEXT", L"MODE_TEXT", 0);
	}
	else if (hook_mode == 2) {
		MessageBox(0, L"MODE_DATA", L"MODE_DATA", 0);
	}
	*/

	return 0;
}
